{
  "name": "flowrad-devlog-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dev-loop relay: bridges the FlowRad Mic app (WebSocket, outbound) to Claude (MCP over HTTP). In-memory only, no DB.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  }
}
